Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files/dirs directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything else to index.html (React Router SPA)
RewriteRule ^ index.html [L]
